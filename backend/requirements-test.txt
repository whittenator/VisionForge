# Lightweight dependency set for running the backend unit tests + linters in CI.
#
# The unit tests never import the ML stack (torch / torchvision / ultralytics /
# onnx / onnxruntime / open-clip / opencv / pillow): those are imported lazily
# inside service functions and are stubbed in the tests. Installing the full
# ~1.5GB ML stack on every PR only makes CI slow and fragile for zero unit-test
# value. Integration/perf tests that exercise the real ML stack run against
# live services outside this workflow.
#
# Keep this in sync with the non-ML runtime dependencies in requirements.txt.
fastapi>=0.112
uvicorn[standard]>=0.30
pydantic[email]>=2.7
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
celery>=5.3
redis>=5.0
minio>=7.2
prometheus-client>=0.20
structlog>=24.1
httpx>=0.27
pytest>=8.2
pytest-cov>=5.0
pgvector>=0.2
python-jose[cryptography]>=3.3
bcrypt>=4.1,<6
python-multipart>=0.0.9
slowapi>=0.1
